# A smart contract for digital assets

The smart contract is to enable users to swap off-chain (USD) for on-chain (APT) assets and vice versa.

## Test with Apto command

```console
$ aptos move test
```